# --- html-deck-v5 skill: ship the TOOL only ---

# Deck work products (each user generates their own; not part of the tool)
decks/

# Python bytecode caches
__pycache__/
*.pyc
*.pyo

# Local runtime cache / state the tool creates on the author's machine
.local/

# Temp files + OS cruft
*.tmp
*.tmp.*
.DS_Store
Thumbs.db
